# secrets -- never commit an api key
.env
.env.*
!.env.example

# maven build output
target/
*/target/

# the packaged distribution: the launcher and config are tracked, the jar is not
greenfinger/bin/*.jar
greenfinger/bin/logs/

# crawl output and the resumable state beside it
data/
logs/
.state/
.rocksdb/

# IDE
.idea/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
.factorypath
.vscode/

# OS
.DS_Store

# Other
change_logs


# What flatten-maven-plugin writes beside each pom so the installed one carries the resolved
# ${revision} rather than the literal. A build artefact, regenerated every run.
.flattened-pom.xml
